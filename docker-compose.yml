services:
  server:
    build:
      context: ./backend
    ports:
      - "3000:3000"
    env_file:
      - ./backend/.env

  client:
    build:
      context: ./client
    ports:
      - "5173:5173"
